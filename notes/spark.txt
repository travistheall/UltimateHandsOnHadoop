spark is 100x faster than mapreduce in memory and 10x on a disk
uses directed acyclic graph to optimze workflows
spark very active ecosystem real world data sets
all revolves around a RDD (resilient distributed dataset)
